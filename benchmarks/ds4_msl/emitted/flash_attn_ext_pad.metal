// Generated by tile-rs mlir_to_msl — DO NOT EDIT
// Compile: xcrun metal -c <file>.metal -o <file>.air && xcrun metallib <file>.air -o <file>.metallib
// Runtime: metal::Device::new_library_with_source(src, &CompileOptions::new())

#include <metal_stdlib>
using namespace metal;

kernel void ds4_flash_attn_ext_pad(
    device const  char* p0 [[ buffer(0) ]],
    device const  char* p1 [[ buffer(1) ]],
    device const  char* p2 [[ buffer(2) ]],
    device  char* p3 [[ buffer(3) ]],
    constant uint& ne11         [[ buffer(4) ]],
    constant uint& ne_12_2      [[ buffer(5) ]],
    constant uint& ne_12_3      [[ buffer(6) ]],
    constant uint& nb11         [[ buffer(7) ]],
    constant uint& nb12         [[ buffer(8) ]],
    constant uint& nb13         [[ buffer(9) ]],
    constant uint& nb21         [[ buffer(10) ]],
    constant uint& nb22         [[ buffer(11) ]],
    constant uint& nb23         [[ buffer(12) ]],
    constant uint& ne31         [[ buffer(13) ]],
    constant uint& ne32         [[ buffer(14) ]],
    constant uint& ne33         [[ buffer(15) ]],
    constant uint& nb31         [[ buffer(16) ]],
    constant uint& nb32         [[ buffer(17) ]],
    constant uint& nb33         [[ buffer(18) ]],
    constant uint& has_mask     [[ buffer(19) ]],
    constant uint& c_ncpsg      [[ buffer(20) ]],
    uint3 _tid_v   [[ thread_position_in_threadgroup ]],
    uint3 tgpig    [[ threadgroup_position_in_grid ]],
    uint3 _tc_v    [[ threads_per_threadgroup ]]
)
{

    uint tiitg = _tid_v.x;
    uint ntg_x = _tc_v.x;
    uint C = c_ncpsg;
    uint i1 = tgpig.x;
    uint i2 = tgpig.y;
    uint i3 = tgpig.z;

    device char * k_pad    = p3;
    device char * v_pad    = k_pad + nb11 * C * ne_12_2 * ne_12_3;
    device char * mask_pad = v_pad + nb21 * C * ne_12_2 * ne_12_3;

    int icp = (int)ne11 % (int)C;
    int ic0 = (int)ne11 - icp;

    if (i2 < ne_12_2 && i3 < ne_12_3) {
        device const char * k_src = p0 + nb11 * (uint64_t)(ic0 + (int)i1) + nb12 * i2 + nb13 * i3;
        device const char * v_src = p1 + nb21 * (uint64_t)(ic0 + (int)i1) + nb22 * i2 + nb23 * i3;

        device char * k_dst = k_pad + nb11 * i1 + nb11 * C * i2 + nb11 * C * ne_12_2 * i3;
        device char * v_dst = v_pad + nb21 * i1 + nb21 * C * i2 + nb21 * C * ne_12_2 * i3;

        if ((int)i1 >= icp) {
            for (uint i = tiitg; i < nb11; i += ntg_x) { k_dst[i] = (char)0; }
            for (uint i = tiitg; i < nb21; i += ntg_x) { v_dst[i] = (char)0; }
        } else {
            for (uint i = tiitg; i < nb11; i += ntg_x) { k_dst[i] = k_src[i]; }
            for (uint i = tiitg; i < nb21; i += ntg_x) { v_dst[i] = v_src[i]; }
        }
    }

    if (has_mask != 0u) {
        if (i2 < ne32 && i3 < ne33) {
            for (uint ib = i1; ib < ne31; ib += C) {
                device const half * mask_src = (device const half *)(p2 + nb31 * ib + nb32 * i2 + nb33 * i3) + ic0;
                device       half * mask_dst = (device       half *)(mask_pad) + C * ib + C * ne31 * i2 + C * ne31 * ne32 * i3;
                for (uint i = tiitg; i < C; i += ntg_x) {
                    if ((int)i >= icp) { mask_dst[i] = -MAXHALF; }
                    else            { mask_dst[i] = mask_src[i]; }
                }
            }
        }
    }
}

