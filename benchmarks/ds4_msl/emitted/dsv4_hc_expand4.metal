// Generated by tile-rs mlir_to_msl — DO NOT EDIT
// Compile: xcrun metal -c <file>.metal -o <file>.air && xcrun metallib <file>.air -o <file>.metallib
// Runtime: metal::Device::new_library_with_source(src, &CompileOptions::new())

#include <metal_stdlib>
using namespace metal;

kernel void ds4_dsv4_hc_expand4(
    device const  char* p0 [[ buffer(0) ]],
    device const  char* p1 [[ buffer(1) ]],
    device const  char* p2 [[ buffer(2) ]],
    device const  char* p3 [[ buffer(3) ]],
    device const  char* p4 [[ buffer(4) ]],
    device  char* p5 [[ buffer(5) ]],
    constant uint& n_embd     [[ buffer(6) ]],
    constant uint& n_hc       [[ buffer(7) ]],
    constant uint& n_tokens   [[ buffer(8) ]],
    constant uint& nb_block0  [[ buffer(9) ]],
    constant uint& nb_block1  [[ buffer(10) ]],
    constant uint& nb_add0    [[ buffer(11) ]],
    constant uint& nb_add1    [[ buffer(12) ]],
    constant uint& nb_res0    [[ buffer(13) ]],
    constant uint& nb_res1    [[ buffer(14) ]],
    constant uint& nb_res2    [[ buffer(15) ]],
    constant uint& nb_post0   [[ buffer(16) ]],
    constant uint& nb_post1   [[ buffer(17) ]],
    constant uint& nb_comb0   [[ buffer(18) ]],
    constant uint& nb_comb1   [[ buffer(19) ]],
    constant uint& nb_comb2   [[ buffer(20) ]],
    constant uint& nb0        [[ buffer(21) ]],
    constant uint& nb1        [[ buffer(22) ]],
    constant uint& nb2        [[ buffer(23) ]],
    constant uint& has_add    [[ buffer(24) ]],
    uint tid   [[ thread_position_in_threadgroup ]],
    uint row   [[ threadgroup_position_in_grid ]],
    uint tcount [[ threads_per_threadgroup ]]
)
{

    if (n_hc != 4u) return;
    uint gid = row * tcount + tid;
    uint total = n_embd * n_tokens;
    if (gid >= total) return;
    uint d = gid % n_embd;
    uint t = gid / n_embd;

    float block_v = *((device const float *)(p0 + (uint64_t)d * nb_block0 + (uint64_t)t * nb_block1));
    if (has_add != 0u) {
        block_v += *((device const float *)(p4 + (uint64_t)d * nb_add0 + (uint64_t)t * nb_add1));
    }

    const float r0 = *((device const float *)(p1 + (uint64_t)d * nb_res0 + 0u * nb_res1 + (uint64_t)t * nb_res2));
    const float r1 = *((device const float *)(p1 + (uint64_t)d * nb_res0 + 1u * nb_res1 + (uint64_t)t * nb_res2));
    const float r2 = *((device const float *)(p1 + (uint64_t)d * nb_res0 + 2u * nb_res1 + (uint64_t)t * nb_res2));
    const float r3 = *((device const float *)(p1 + (uint64_t)d * nb_res0 + 3u * nb_res1 + (uint64_t)t * nb_res2));

    for (uint dst_hc = 0u; dst_hc < 4u; ++dst_hc) {
        float acc = block_v * *((device const float *)(p2 + (uint64_t)dst_hc * nb_post0 + (uint64_t)t * nb_post1));
        acc += *((device const float *)(p3 + (uint64_t)dst_hc * nb_comb0 + 0u * nb_comb1 + (uint64_t)t * nb_comb2)) * r0;
        acc += *((device const float *)(p3 + (uint64_t)dst_hc * nb_comb0 + 1u * nb_comb1 + (uint64_t)t * nb_comb2)) * r1;
        acc += *((device const float *)(p3 + (uint64_t)dst_hc * nb_comb0 + 2u * nb_comb1 + (uint64_t)t * nb_comb2)) * r2;
        acc += *((device const float *)(p3 + (uint64_t)dst_hc * nb_comb0 + 3u * nb_comb1 + (uint64_t)t * nb_comb2)) * r3;
        *((device float *)(p5 + (uint64_t)d * nb0 + (uint64_t)dst_hc * nb1 + (uint64_t)t * nb2)) = acc;
    }
}

