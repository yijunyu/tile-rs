// Generated by tile-rs mlir_to_msl — DO NOT EDIT
// Compile: xcrun metal -c <file>.metal -o <file>.air && xcrun metallib <file>.air -o <file>.metallib
// Runtime: metal::Device::new_library_with_source(src, &CompileOptions::new())

#include <metal_stdlib>
using namespace metal;

kernel void ds4_rope(
    device const  float* p0 [[ buffer(0) ]],
    device  float* p1 [[ buffer(1) ]],
    constant uint& rows         [[ buffer(2) ]],
    constant uint& cols         [[ buffer(3) ]],
    constant uint& pos          [[ buffer(4) ]],
    uint tid   [[ thread_position_in_threadgroup ]],
    uint row   [[ threadgroup_position_in_grid ]],
    uint tcount [[ threads_per_threadgroup ]]
)
{

    // RoPE: apply cos/sin rotation for each pair of dims
    uint r = row;
    if (r >= rows) return;
    for (uint i = tid; i < cols / 2; i += tcount) {
        float freq = 1.0f / pow(10000.0f, 2.0f * (float)i / (float)cols);
        float angle = (float)pos * freq;
        float c = cos(angle);
        float s = sin(angle);
        float x0 = p0[r * cols + 2 * i];
        float x1 = p0[r * cols + 2 * i + 1];
        p1[r * cols + 2 * i]     = (float) (x0 * c - x1 * s);
        p1[r * cols + 2 * i + 1] = (float) (x0 * s + x1 * c);
    }
}

