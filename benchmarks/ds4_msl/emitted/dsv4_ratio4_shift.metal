// Generated by tile-rs mlir_to_msl — DO NOT EDIT
#include <metal_stdlib>
using namespace metal;

kernel void ds4_dsv4_ratio4_shift(
    device  float* p0 [[ buffer(0) ]],
    device  float* p1 [[ buffer(1) ]],
    constant uint& width        [[ buffer(2) ]],
    uint tid   [[ thread_position_in_threadgroup ]],
    uint row   [[ threadgroup_position_in_grid ]],
    uint tcount [[ threads_per_threadgroup ]]
)
{
    uint gid  = row * tcount + tid;
    uint n4   = width;          // n/4 == 4*width/4 == width float4 lanes per buffer
    if (gid >= n4) return;
    device float4* p0v = reinterpret_cast<device float4*>(p0);
    device float4* p1v = reinterpret_cast<device float4*>(p1);
    p0v[gid] = p0v[n4 + gid];
    p1v[gid] = p1v[n4 + gid];
}
