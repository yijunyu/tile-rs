// Generated by tile-rs mlir_to_msl — DO NOT EDIT
// Compile: xcrun metal -c <file>.metal -o <file>.air && xcrun metallib <file>.air -o <file>.metallib
// Runtime: metal::Device::new_library_with_source(src, &CompileOptions::new())

#include <metal_stdlib>
using namespace metal;

kernel void ds4_dsv4_softmax_pool(
    device const  float* p0 [[ buffer(0) ]],
    device const  float* p1 [[ buffer(1) ]],
    device  float* p2 [[ buffer(2) ]],
    constant uint& ne00         [[ buffer(3) ]],
    constant uint& ne0          [[ buffer(4) ]],
    constant uint& ne1          [[ buffer(5) ]],
    uint tid   [[ thread_position_in_threadgroup ]],
    uint row   [[ threadgroup_position_in_grid ]],
    uint tcount [[ threads_per_threadgroup ]]
)
{

    uint gid = row * tcount + tid;
    uint total = ne0 * ne1;
    if (gid >= total) return;
    uint id = gid % ne0;
    uint ic = gid / ne0;
    uint base = ic * ne0 * ne00 + id * ne00;
    float max_s = -INFINITY;
    for (uint ir = 0; ir < ne00; ir++) {
        float s = p1[base + ir];
        max_s = max(max_s, s);
    }
    float sum = 0.0;
    float acc = 0.0;
    for (uint ir = 0; ir < ne00; ir++) {
        float w = exp(p1[base + ir] - max_s);
        sum += w;
        acc += p0[base + ir] * w;
    }
    p2[ic * ne0 + id] = acc / sum;
}

