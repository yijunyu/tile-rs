# `tile` — specification coverage

Regenerated by `cargo test --test spec`. An untagged scenario must be
green; everything else carries `@planned` and is counted, not ignored.

| feature | live | total | |
|---|---:|---:|---|
| `01_form_detection.feature` | 6 | 6 | ████████████████████ |
| `02_transform_dispatch.feature` | 18 | 21 | █████████████████··· |
| `03_intermediates.feature` | 7 | 7 | ████████████████████ |
| `04_profile_and_optimize.feature` | 25 | 25 | ████████████████████ |
| `05_platform_and_targets.feature` | 9 | 10 | ██████████████████·· |
| `06_toolchains.feature` | 14 | 14 | ████████████████████ |
| `07_stats_and_license.feature` | 15 | 15 | ████████████████████ |
| `08_daemon_and_mcp.feature` | 15 | 15 | ████████████████████ |
| `09_ui.feature` | 10 | 10 | ████████████████████ |
| `10_portability_and_grammar.feature` | 9 | 10 | ██████████████████·· |
| `11_io_edges.feature` | 16 | 16 | ████████████████████ |
| `12_run.feature` | 20 | 20 | ████████████████████ |
| `13_backlog.feature` | 12 | 12 | ████████████████████ |

The rest, by reason — these are different kinds of thing:

* `@requires-backend` — 1: conditional: the codegen backend is provisioned here, so this RAN
* `@requires-cross` — 1: conditional: compiles for another triple (minutes); set TILE_SPEC_CROSS=1
* `@requires-device` — 4: conditional: needs a device — present here, so these RAN
* `@requires-hosts` — 1: conditional: needs other machines; set TILE_SPEC_HOSTS="mac mini"
* `@requires-lifter` — 3: needs a frontend; a source form owes no reader, so this is a decision, not pending work
* `@requires-stats-build` — 4: conditional: needs the crypto feature — built in, so these RAN
* `@requires-toolchain` — 1: conditional: the target compiler is here, so this RAN
* `@requires-wasm-runtime` — 1: conditional: a wasm runtime is here, so the bundle's behaviour RAN
* `@requires-wasm-target` — 1: conditional: the wasm target is here, so the bundle was rebuilt and compared

**176 of 181 scenarios live (97%)**, 241 cases executed green.
